syntax = "proto2";

package efficient_paper;

message PaperInfo {
    optional Paper paper = 1;
    optional Publication pub = 2;
    optional Code code = 3;
    optional Note note = 4;
    optional Keyword keyword = 5;
    optional Cover cover = 6;
    optional Baseline baseline = 7;
    optional int64 update_time = 8;  // Unix timestamp when paper was added/updated
}

message Paper {
    optional string title = 1 [default="paper title"];
    optional string abbr = 2 [default=""];
    optional string url = 3;
    repeated string authors = 4;
    repeated string institutions = 5;
}

message Publication {
    optional string where = 1 [default="arXiv"];
    optional int32 year = 2;
}

message Code {
    optional string type = 1 [default="Pytorch"];
    optional string url = 2;
}

message Note {
    optional string url = 1 [default="note.md"];
}

message Cover {
    optional string url = 1;
}

message Baseline {
    repeated string methods = 1;
}

message Keyword {
    repeated Word words = 1;
    enum Word {
        none = 1;
        sparse_pruning = 2;
        attention_sparsity = 3;
        activation_sparsity = 4;
        weight_sparsity = 5;
        structured_sparsity = 6;

        quantization = 10;

        kv_cache_quant = 20;
        kv_cache_sparse = 21;
        kv_cache_management = 22;

        overlap = 30;
        speculative_decoding = 31;

        performance_modeling = 40;
        deployment = 41;
        survey = 42;
        structure_design = 43;
        low_rank = 44;
        fusion = 45;
        efficient_training = 46;
        tool = 47;
        benchmark = 48;
        // TODO: please add your key words here.
    }
}

message SearchWord {
    repeated string background_words = 1;
    repeated string key_words = 2;
    repeated string exclude_words = 3;
}

message RepoList {
    repeated string repo_url = 1;
}